name: Automatisation des tests DevSecOps

on: 
  push:
    branches: [ master, main ]

jobs:
  Dependances:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Configuration de Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Contrôle des dépendances avec Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: 'requirements.txt'
          format: 'json'
          output: 'trivy-dependencies-report.json'
          exit-code: '0'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Génération du rapport HTML des dépendances
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: 'requirements.txt'
          format: 'html'
          output: 'trivy-dependencies-report.html'
          exit-code: '0'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Upload du rapport de dépendances
        uses: actions/upload-artifact@v4
        with:
          name: rapport-dependances
          path: |
            trivy-dependencies-report.json
            trivy-dependencies-report.html

  Dockerfile:
    runs-on: ubuntu-latest
    needs: Dependances
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Vérification de l'existence du Dockerfile
        run: |
          if [ ! -f Dockerfile ]; then
            echo "Création d'un Dockerfile..."
            cat > Dockerfile << 'EOF'
          FROM python:3.11-slim
          WORKDIR /app
          RUN useradd -m -u 1000 flaskuser && chown -R flaskuser:flaskuser /app
          COPY requirements.txt .
          RUN pip install --no-cache-dir -r requirements.txt
          COPY . .
          USER flaskuser
          EXPOSE 4000
          CMD ["python", "main.py"]
          EOF
          fi

      - name: Construction de l'image Docker
        run: |
          docker build -t breakableflask:${{ github.sha }} .

      - name: Test Dockerfile avec Trivy (JSON)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'breakableflask:${{ github.sha }}'
          format: 'json'
          output: 'trivy-image-report.json'
          exit-code: '0'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Test Dockerfile avec Trivy (HTML)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'breakableflask:${{ github.sha }}'
          format: 'html'
          output: 'trivy-image-report.html'
          exit-code: '0'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Upload du rapport Docker
        uses: actions/upload-artifact@v4
        with:
          name: rapport-docker
          path: |
            trivy-image-report.json
            trivy-image-report.html

  Deploy:
    runs-on: ubuntu-latest
    needs: Dockerfile
    steps:
      - name: Téléchargement des rapports
        uses: actions/download-artifact@v4

      - name: Résumé des rapports disponibles
        run: |
          echo "Rapports de sécurité générés :"
          echo ""
          echo "1. Rapport des dépendances Python :"
          echo "   - trivy-dependencies-report.json"
          echo "   - trivy-dependencies-report.html"
          echo ""
          echo "2. Rapport de l'image Docker :"
          echo "   - trivy-image-report.json"
          echo "   - trivy-image-report.html"
          echo ""
          echo "Tous les tests de sécurité sont terminés"
          echo ""
